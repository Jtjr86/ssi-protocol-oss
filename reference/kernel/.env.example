# SSI Cloud Kernel - Environment Configuration
# ⚠️ FOR DEVELOPMENT AND PILOT EVALUATION ONLY

# Kernel Configuration
PORT=5050

# Optional: Logging
LOG_LEVEL=info

# SECURITY NOTES:
# 1. Copy this file to .env if you need custom configuration
# 2. The Kernel doesn't currently require secrets, but use .env for local overrides
# 3. Keep .env out of version control (it's in .gitignore)
